import React from 'react'

const Profile = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
            <p>Profile Layout</p>
        </>
    )
}

export default Profile